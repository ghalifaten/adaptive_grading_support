// assignment (the exercise itself)
// student
// submission

class Submission {
    constructor(author, text, feedback, grade) {
        this.author = author;
        this.text = text;
        this.feedback = feedback;
        this.grade = grade;
    }
}